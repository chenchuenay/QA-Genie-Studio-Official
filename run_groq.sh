#!/bin/bash
flutter run --dart-define=AI_PROVIDER=groq




Analyze the uploaded FULL QA Genie Flutter project and behave as a principal-level Flutter + Android production engineer.

DO NOT only provide recommendations.

I want:

* actual improved code
* rewritten files
* optimized implementations
* production-safe patches
* refactored widgets
* architecture fixes
* performance fixes
* DB migration fixes
* state management fixes
* export hardening fixes
* dropdown normalization fixes
* parser hardening fixes
* realism engine improvements
* token budgeting improvements
* UI virtualization improvements
* scroll/render optimizations
* memory optimizations
* monetization-safe improvements
* AI prompt engineering improvements
* offline resilience improvements

IMPORTANT:
You MUST directly generate:

* improved Dart files
* modified Flutter widgets
* optimized services
* migration patches
* architecture refactors
* fixed implementations

NOT just theoretical advice.

PROJECT CONTEXT:
QA Genie is an AI-powered manual QA testcase generation app.

Core flow:
User Input
→ Single Gemini API Request
→ Parser
→ Deterministic Repair
→ Structural Validation
→ Fallback Recovery
→ SQLite Persistence
→ Editable Preview
→ Export

STRICT RULES:

1. ONLY ONE AI REQUEST PER GENERATION
2. No multi-agent orchestration
3. No second AI repair pass
4. No recursive prompting
5. No expensive cloud architecture
6. Must remain mobile-first
7. Must remain export-safe
8. Must preserve current UI identity
9. Must preserve current workflow
10. Must support offline fallback generation

IMPORTANT APP CHARACTERISTICS:

* Large editable tables
* Dynamic dropdown editing
* Real-time testcase mutations
* SQLite persistence
* Heavy export workflows
* AI realism generation
* Rewarded ads
* Interstitial ads
* Sponsored suite cards
* Free vs Pro generation tiers
* Dark glossy cyber UI

CURRENT FEATURES:

* AI testcase generation
* Offline fallback generation
* Excel export
* Jira export
* Xray export
* PDF export
* Summary reports
* Editable execution workflow
* History/suites persistence
* Realism engine
* Structural validators
* Truncation recovery
* Telemetry counters
* SQLite locking

CURRENT KNOWN ISSUES:

1. Dropdown duplication crashes
2. Runtime hydration inconsistencies
3. Large table rendering lag
4. Export memory spikes
5. Long AI response truncation
6. Repetitive realism phrasing
7. Scroll jank in preview/master table
8. Potential memory leaks
9. SQLite migration fragility
10. Weak virtualization for huge suites

REQUIRED OUTPUT:
I want you to:

1. READ ALL IMPORTANT FILES
2. UNDERSTAND CURRENT ARCHITECTURE
3. MODIFY FILES DIRECTLY
4. RETURN IMPROVED FILE CONTENTS
5. CREATE NEW FILES IF NEEDED
6. PRESERVE CURRENT APP IDENTITY
7. PRESERVE EXPORT CONTRACTS
8. PRESERVE DB COMPATIBILITY
9. PRESERVE ONE-REQUEST AI RULE

PRIORITY FIX ORDER:

1. Crash prevention
2. DB consistency
3. Dropdown normalization
4. Render optimization
5. Export stability
6. Realism quality
7. Memory optimization
8. Scalability
9. Monetization safety
10. UX polish

VERY IMPORTANT:
Do NOT suggest rebuilding the app.
Do NOT replace the entire architecture.
Do NOT migrate to expensive backend systems.
Do NOT introduce multi-call AI pipelines.

Instead:
Incrementally harden and evolve the current architecture into production-grade quality.

ANALYZE AND MODIFY:

* lib/core/database/
* lib/core/network/
* lib/engine/
* lib/features/generation/
* lib/features/export/
* lib/features/summary/
* lib/presentation/
* parser pipeline
* validators
* fallback generator
* realism engine
* dropdown state handling
* preview/master table
* export adapters

WHEN POSSIBLE:
Return:

* exact patched Dart files
* exact replacement methods
* exact optimized widgets
* exact migration patches
* exact performance fixes

ALSO:
Evaluate:

* realism quality
* architecture maturity
* scalability
* monetization safety
* production readiness
* engineering maturity

with brutal technical honesty.

IMPORTANT:
This app is intended to become a serious production QA engineering tool, not a demo or AI toy app.