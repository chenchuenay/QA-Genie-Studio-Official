

flutter run --flavor dev

flutter clean
flutter pub get
flutter run --release --flavor prod

  


Testing
fiflutter run --flavor prod --dart-define=MODE=prod

flutter run --flavor dev --dart-define=MODE=dev





'''
build_prod.sh
./build_prod.sh          # builds prod APK
./build_prod.sh appbundle  # builds prod app bundle
run script
./run               → dev debug (default)
./run prod          → prod debug
./run prod-release  → prod release
'''





''' 
## full file dumps 
mkdir -p ~/Desktop/QA_Genie/QAG
adb pull /storage/emulated/0/Download/QA_Genie_Forensics/ ~/Desktop/QA_Genie/QAG
done



























Summary of recommended usage
Purpose	Command
Daily development (with forensic tools)	flutter run
Performance testing (with forensic tools)	flutter run --profile
Building a test APK for QA (with forensic tools)	flutter build apk --debug
Testing production behaviour on a device (no tools)	flutter run --release
Building final customer APK (no tools)	flutter build apk --release








gcloud functions call migrateDataToPods --region=us-central1 --data='{}'



firebase deploy --only functions
firebase functions:log --only generate



Build commands for APKs

bash
# Dev APK
flutter build apk --release --flavor dev

# Prod APK
flutter build apk --release --flavor prod

flutter build appbundle --dart-define=MODE=prod 



adb pull /storage/emulated/0/Download/QA_Genie_Forensics/latest_run.json ~/Desktop/QA_genie/latest_run/
adb pull /storage/emulated/0/Download/QA_Genie_Forensics/latest_run.log ~/Desktop/QA_genie/latest_run/






1. Feature Map & Primary Screens

  ┌──────────────┬────────────────────────────────────┬──────────────────────────────────┐
  │ Feature      │ Primary UI / Screens               │ Purpose                          │
  ├──────────────┼────────────────────────────────────┼──────────────────────────────────┤
  │ Generation   │ home_screen.dart                   │ Main generation interface, batch │
  │              │                                    │ control.                         │
  │ Suites       │ suites_screen.dart,                │ Suite management, list, and      │
  │              │ suite_preview_screen.dart          │ preview/edit.                    │
  │ Monetization │ upgrade_screen.dart,               │ Pro-features, quota limits,      │
  │              │ test_mode_screen.dart              │ debug mode.                      │
  │ Auth         │ auth_dialog.dart                   │ Guest vs. User login management. │
  │ Summary      │ summary_report_preview_screen.dart │ AI report generation & export    │
  │              │                                    │ preview.                         │
  │ Export       │ Export BottomSheet/Dialogs         │ PDF/CSV/Excel output logic.      │
  │ Forensics    │ production_diagnostics_screen.dart │ Internal diagnostic monitoring.  │
  │ Beta/Legal   │ beta_expired_screen.dart           │ Version control & compliance.    │
  │ Support      │ Bug Report screens                 │ Feedback/Issue reporting.        │
  └──────────────┴────────────────────────────────────┴──────────────────────────────────┘
  ---

  2. Core Operational Flows

   * Startup Flow: _StartupGate (Router) → Beta/Version Check → DatabaseService Init → (Auth Dialog)
     → MainScreen (Tab-based navigation).
   * Generation Flow: HomeScreen → (Ad-check/Idempotency check) → functions/generate (Backend) →
     PreviewScreen (Results table).
   * Export Flow: PreviewScreen → ExportBottomSheet → ExportTestCasesUseCase (Backend tracking) →
     SharePlus (Native share).
   * Account/Monetization Flow: AccountScreen (Test Mode) → UpgradeScreen → UsageManager (Quota
     checking).

  ---

  3. Developer/Security Observation (For your "Big Task")

   * Navigation Pattern: Your router is centralized via MainScreen, but individual features manage
     their own navigation stacks (e.g., PreviewScreen pushing SummaryReportScreen).
   * Data Flow: The GenerationSession object acts as the primary data container passed between
     HomeScreen, PreviewScreen, and SummaryReportScreen. It is ephemeral and not stored in
     Firestore, confirming your "No Test Case Storage" policy holds true for the UI layer as well.

  ---

  Ready for the "Big Task"

  I have the full architectural picture. This mapping confirms that the app is modular, with a clean
  separation between the Generation Engine (Engine), Data Persistence (Data), and Features (UI).

  I am ready. What is the Big Task? (Please outline the goal, the constraints, and the specific
  behavior you want to modify or implement.)






 before we continue                                                    
  babe you should act like like developer,tester,project owner,designer,best AI,security head,invester,google playstore app revivuer for approvel,admob empployee who know the policvies who always thinks    
   about cost managment while high revenue white hat ethical hacker to protect our app soo think all combined 
   ad preload is acetable                                                                                 
   understand? 










Dev
Backend:
✓ Firebase Auth
✓ Cloud Functions
✓ Firestore
✓ Gemini
✓ Usage Tracking
✓ Export Tracking
✓ Server-side Limits
✓ Security Checks
full generating dumps to local Storage
latest_run.logiclatest_run.json
debug logs

UI:
✓ Account Test Page
✓ Mock Rewarded Ads
✓ Mock Interstitial Ads
✓ Mock Native Ads

Hidden:
✗ Upgrade Page


Prod
Backend:
✓ Firebase Auth
✓ Cloud Functions
✓ Firestore
✓ Gemini
✓ Usage Tracking
✓ Export Tracking
✓ Server-side Limits
✓ Security Checks

UI:
✓ Upgrade Page
✓ Real Rewarded Ads
✓ Real Interstitial Ads
✓ Real Native Ads
note:currently we using admob test ad units coz we didnt launch app live yet

Hidden:
✗ Account Test Page





  // ============================================================
  // TEMPORARY MIGRATION BUTTON – REMOVE AFTER RUNNING ONCE
  // ============================================================
  Future<void> _runMigration() async {
    final confirmed = await showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        backgroundColor: AppColors.surface,
        title: const Text(
          'Run Migration?',
          style: TextStyle(color: Colors.white),
        ),
        content: const Text(
          'This will restructure your Firestore data (analytics, guests, registry).\n\nOnly run once. Safe to re-run if needed.',
          style: TextStyle(color: AppColors.textSecondary),
        ),
        actions: [
          TextButton(
            onPressed: () => Navigator.pop(ctx, false),
            child: const Text('Cancel'),
          ),
          ElevatedButton(
            onPressed: () => Navigator.pop(ctx, true),
            style: ElevatedButton.styleFrom(backgroundColor: AppColors.accent),
            child: const Text('Run Migration'),
          ),
        ],
      ),
    );
    if (confirmed != true) return;

    // Show loading indicator
    ScaffoldMessenger.of(
      context,
    ).showSnackBar(const SnackBar(content: Text('Running migration...')));

    try {
      final result = await FunctionsService.call(
        functionName: 'migrateDataToPods',
        payload: {},
      );
      debugPrint('✅ Migration result: $result');
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(content: Text('Migration completed successfully!')),
        );
      }
    } catch (e) {
      debugPrint('❌ Migration error: $e');
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(
            content: Text('Migration failed: $e'),
            backgroundColor: AppColors.error,
          ),
        );
      }
    }
  }



  GEMINI_API_KEY=AQ.Ab8RN6ItebfqyWP5cbn6RFfO0i35vUO_46Kzj21U8hsCBGymYw
GEMINI_BASE_URL=https://generativelanguage.googleapis.com


GROQ_API_KEY=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


DEEPSEEK_API_KEY=sk-6696e156f808472fac6c72c5e0b2ac56

