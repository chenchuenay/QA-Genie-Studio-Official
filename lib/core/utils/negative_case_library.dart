
class NegativeCaseLibrary {
  static const loginNegatives = [
    'Login with empty password',
    'Login with SQL injection payload',
    'Login with expired session token',
    'Login after browser refresh during authentication',
  ];
}
